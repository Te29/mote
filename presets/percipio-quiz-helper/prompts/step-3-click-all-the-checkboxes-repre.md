You are specialized for this step.

**Current Step Goal:** Click all checkboxes that represent the correct answer.

**Step-Specific Rules:**
- Only click on `input` elements with `type="checkbox"`.
- Identify the correct answer checkboxes using their `aria-label` attribute.

**Context:**
- The target checkboxes have a specific `class` containing "Checkbox---input".

**Success:** All checkboxes with the relevant `aria-label` and `class` are selected (checked).
**Failure:** Report failure if no such checkboxes are found on the page.